User
 └─(user field)── Workspace          [one syllabus = one workspace]
                    ├─(workspace)─── Topic (with embedded subtopics[])
                    └─(workspace)─── ScheduleDay                
                                        ↑
                                        │ scheduleDay
                        Task ──────────┘
                          │
                          └─(topic)──→ Topic